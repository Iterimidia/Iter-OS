import { FilesBrowser } from '@/components/files/FilesBrowser'

export function FilesPage() {
  return <FilesBrowser title="Arquivos" description="Links organizados por categoria — o Iter OS não substitui o Drive." />
}
